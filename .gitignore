# OS / Editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node (harmless even if unused)
node_modules/

# Terraform (do NOT commit state, plans, or local cache)
**/.terraform/*
*.tfstate
*.tfstate.*
terraform.tfstate.backup
*.tfplan
crash.log
crash.*.log
.terraformrc
terraform.rc

# Terraform variables (often contain secrets)
*.tfvars
*.tfvars.json

# Secrets (never commit)
.env
.env.*
*.pem
*.key
**/key.json

# Build / Artifacts
dist/
build/
*.zip
out*.json
